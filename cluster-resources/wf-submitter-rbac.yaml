# Identity used by parent-ns workloads to submit workflows into tenant
# namespaces through the argo-server API.
#
# With argo-server running in `--auth-mode=client`, the caller's own
# ServiceAccount token is what authorises the request: argo-server builds a
# Kubernetes client from that token, so every permission below must be held
# by this SA, not by argo-server.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: wf-submitter
  namespace: parent-ns
---
# ClusterWorkflowTemplates are cluster-scoped, so reading the template being
# submitted needs a ClusterRole rather than a namespaced Role.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: argo-clusterworkflowtemplate-reader
rules:
  - apiGroups: ["argoproj.io"]
    resources: ["clusterworkflowtemplates"]
    verbs: ["get", "list"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: wf-submitter-reads-clusterworkflowtemplates
subjects:
  - kind: ServiceAccount
    name: wf-submitter
    namespace: parent-ns
roleRef:
  kind: ClusterRole
  name: argo-clusterworkflowtemplate-reader
  apiGroup: rbac.authorization.k8s.io
---
# Permission to actually run workflows lands in tenant-ns. Scoped to submit
# and observe — deliberately no delete, and no access to tenant Secrets.
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: argo-workflow-submitter
  namespace: tenant-ns
rules:
  - apiGroups: ["argoproj.io"]
    resources: ["workflows"]
    verbs: ["create", "get", "list", "watch"]
  # The argo-server log endpoint streams on the caller's behalf: it watches
  # pods to follow them, so `pods/log` alone yields
  # "cannot watch resource pods". Both rules are required.
  #
  # Cost of granting these: the caller can read anything a tenant workflow
  # prints, and can see tenant pod specs — which expose the NAMES of mounted
  # Secrets and their keys, though not their values. Drop both rules if
  # submitters should be write-only and blind to output.
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch"]
  - apiGroups: [""]
    resources: ["pods/log"]
    verbs: ["get"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: parent-ns-submits-to-tenant-ns
  namespace: tenant-ns
subjects:
  # Subject lives in parent-ns; the permissions apply in tenant-ns.
  - kind: ServiceAccount
    name: wf-submitter
    namespace: parent-ns
roleRef:
  kind: Role
  name: argo-workflow-submitter
  apiGroup: rbac.authorization.k8s.io
